% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_distribucion.R
\name{general_distribucion}
\alias{general_distribucion}
\title{Distribución de puntajes (General o por pilares) en una edición}
\usage{
general_distribucion(
  edicion,
  modo = c("pilares", "general"),
  regiones = "ALL",
  usar_codigos = TRUE,
  tipo = c("boxplot", "violin"),
  jitter = FALSE,
  paleta = c("blues", "viridis", "cividis"),
  linea_promedio = TRUE,
  mostrar_leyenda = FALSE
)
}
\arguments{
\item{edicion}{Entero de longitud 1 (p. ej., 2025). **Obligatorio**.}

\item{modo}{`"general"` (un solo eje con el índice general) o `"pilares"`
(un eje por cada pilar). Por defecto `"pilares"`.}

\item{regiones}{Vector de regiones a incluir (códigos, nombres o grupos como `"gr_costa"`).
Use `"ALL"` para todas.}

\item{usar_codigos}{Si `TRUE`, traduce códigos de regiones a nombres oficiales.}

\item{tipo}{`"boxplot"` (default) o `"violin"`.}

\item{jitter}{`TRUE/FALSE` para superponer puntos individuales (alpha suave).}

\item{paleta}{Paleta de relleno: `"blues"` (default), `"viridis"`, `"cividis"`.
En `modo="pilares"` se aplica por **pilar**; en `modo="general"` el relleno es fijo.}

\item{linea_promedio}{`TRUE/FALSE`. Dibuja línea horizontal punteada del promedio nacional.}

\item{mostrar_leyenda}{`TRUE/FALSE`. Por defecto `FALSE`.}
}
\value{
Un objeto `ggplot2`.
}
\description{
Para una **edición** del INCORE, muestra la dispersión de puntajes entre
regiones como **boxplot** o **violin**. Puede graficar el **índice general**
(un solo eje, código **GEN**) o **los seis pilares** (códigos **ECO/LAB/INF/SAL/EDU/INS**).
}
\details{
Siempre usa `leer_incore(..., agregar_codigos = TRUE)` y solo filas con
`Unidad == "Puntaje del 0 al 10"`.
}
