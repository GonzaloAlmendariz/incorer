% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indc_barras.R
\name{indc_barras}
\alias{indc_barras}
\title{Barras de un indicador por región (una edición)}
\usage{
indc_barras(
  edicion,
  pilar = NULL,
  indicador = NULL,
  regiones = "ALL",
  usar_codigos = TRUE,
  incluir_peru = FALSE,
  paleta = c("ipe", "okabe_ito", "viridis"),
  mostrar_leyenda = FALSE
)
}
\arguments{
\item{edicion}{integer(1). Año de edición a graficar (p. ej., 2025). Obligatorio.}

\item{pilar}{character(1). Código o nombre del pilar (p.ej. "LAB" o "Laboral"). Opcional
si `indicador` es un código/nombre único.}

\item{indicador}{character(1). Código o nombre completo del indicador
(p.ej. "LAB2" o "2.2 Formalidad laboral"). **Recomendable especificarlo**.}

\item{regiones}{character(). Vector de regiones o grupos; use `"ALL"` para todas.}

\item{usar_codigos}{logical. Si `TRUE`, traduce códigos de regiones/pilar/indicador.}

\item{incluir_peru}{logical. Si `TRUE`, incluye "Perú" (promedio nacional). Default `FALSE`.}

\item{paleta}{character(1). Paleta cualitativa: `"ipe"` (default), `"okabe_ito"`, `"viridis"`.}

\item{mostrar_leyenda}{logical. Mostrar/ocultar leyenda (default `FALSE`).}
}
\value{
Un objeto `ggplot2`.
}
\description{
Grafica barras (coord_flip) del **puntaje (0–10)** de un **indicador**
específico del INCORE para **una** edición. Ordena de mayor a menor y
colorea por región con paletas profesionales. Soporta:
  - Filtros de regiones con grupos `gr_*` y exclusiones con prefijo `-`
  - Códigos o nombres para pilar/indicador (vía catálogos)
}
