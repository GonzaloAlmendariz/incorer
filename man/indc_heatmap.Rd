% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indc_heatmap.R
\name{indc_heatmap}
\alias{indc_heatmap}
\title{Heatmap de puntajes por indicadores o por tiempo (INCORE)}
\usage{
indc_heatmap(
  modo = c("indicadores", "tiempo"),
  pilar = NULL,
  indicador = NULL,
  edicion = NULL,
  ediciones = NULL,
  regiones = "ALL",
  usar_codigos = TRUE,
  ordenar = c("ninguno", "por_ultimo", "por_promedio"),
  paleta = c("blues", "viridis", "cividis", "magma"),
  anotar = FALSE,
  mostrar_leyenda = TRUE
)
}
\arguments{
\item{modo}{"indicadores" (columnas = indicadores del pilar, una edición)
o "tiempo" (columnas = ediciones, un indicador).}

\item{pilar}{character(1). Código o nombre del pilar (requerido si `modo="indicadores"`).}

\item{indicador}{character(1). Código o nombre del indicador (requerido si `modo="tiempo"`).}

\item{edicion}{integer(1). Edición cuando `modo="indicadores"`.}

\item{ediciones}{integer(n>=2). Rango de ediciones cuando `modo="tiempo"`.}

\item{regiones}{"ALL" o vector de regiones (códigos, nombres o grupos `gr_*`;
admite exclusiones con prefijo "-").}

\item{usar_codigos}{logical. Si TRUE, admite filtros por códigos de región/pilar/indicador.}

\item{ordenar}{"ninguno","por_ultimo","por_promedio" (solo en `modo="tiempo"`).}

\item{paleta}{"blues","viridis","cividis","magma".}

\item{anotar}{logical. Escribir el valor dentro de cada celda.}

\item{mostrar_leyenda}{logical.}
}
\value{
Un objeto `ggplot2`.
}
\description{
Dibuja un mapa de calor con filas = regiones. Las columnas pueden ser:
- **Indicadores** de un pilar en **una** edición (`modo = "indicadores"`).
- **Ediciones** para **un** indicador (`modo = "tiempo"`).
}
\details{
Siempre usa `indc_leer_incore(..., agregar_codigos = TRUE)` para contar con
columnas de códigos (`pilar_cod`, `ind_cod`) y trabajar solo con puntajes 0–10.
En ambos modos se excluye el indicador "General".
}
