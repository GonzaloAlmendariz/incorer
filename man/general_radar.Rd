% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_radar.R
\name{general_radar}
\alias{general_radar}
\title{Telaraña (radar) del puntaje General por pilares (una edición)}
\usage{
general_radar(
  edicion,
  regiones = "AUTO3",
  usar_codigos = TRUE,
  max_regiones = 6,
  alpha = 0.25,
  paleta = c("ipe", "okabe_ito", "viridis"),
  mostrar_puntos = TRUE,
  etiquetas_pilares = c("corto", "largo")
)
}
\arguments{
\item{edicion}{integer(1). Edición (p. ej., 2025).}

\item{regiones}{Vector de regiones (códigos o nombres).
Use `"AUTO3"` para la selección automática bajo/medio/alto; `NULL` muestra todas.}

\item{usar_codigos}{logical. Si TRUE, traduce códigos de región antes de filtrar.}

\item{max_regiones}{Máximo recomendado antes de advertir (por defecto 6).}

\item{alpha}{Relleno de polígonos (0–1). Default 0.25.}

\item{paleta}{"ipe","okabe_ito","viridis".}

\item{mostrar_puntos}{logical. Puntos en los vértices.}

\item{etiquetas_pilares}{"corto" o "largo".}
}
\value{
ggplot
}
\description{
Dibuja un radar regular (hexágono) con los 6 pilares del INCORE para **una edición**.
Permite comparar varias regiones (mejor 3–6). Si `regiones = "AUTO3"` (default),
se eligen 3 regiones representativas (promedio de pilares más bajo, medio y alto).
}
\details{
Internamente usa `ind_tabla(ediciones = edicion, modo = "pilares", largo = TRUE)`,
y convierte a coordenadas (x, y) para lados rectos (no arcos).
}
