% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importar.R
\name{leer_incore}
\alias{leer_incore}
\title{Leer la base INCORE y devolver SOLO puntajes (0–10)}
\usage{
leer_incore(
  ruta =
    "https://incore-spaces.nyc3.digitaloceanspaces.com/documents/1752079464_INCORE_2025_OpenData_1752079464.xlsx",
  hoja = 1,
  edicion = NULL,
  anio = NULL,
  pilar = NULL,
  indicador = NULL,
  region = NULL,
  usar_codigos = TRUE,
  agregar_codigos = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{ruta}{URL o ruta local al Excel INCORE (por defecto el 2025).}

\item{hoja}{Índice o nombre de hoja (default 1).}

\item{edicion}{(opc) año(s) de edición INCORE (num o char).}

\item{anio}{(opc) año(s) de referencia del dato (num o char).}

\item{pilar}{(opc) pilar(es), códigos (ECO, LAB) o nombres.}

\item{indicador}{(opc) indicador(es), códigos (LAB3) o nombres largos.}

\item{region}{(opc) región(es), códigos (AMZ, AQP, …) o nombres.}

\item{usar_codigos}{Si TRUE, traduce códigos de entrada a nombres antes de filtrar.}

\item{agregar_codigos}{Si TRUE, añade columnas `pilar_cod` e `ind_cod`.}

\item{verbose}{Si TRUE, muestra mensajes.}
}
\value{
tibble con solo puntajes 0–10 (posiblemente filtrado). Si
        `agregar_codigos=TRUE`, incluye `pilar_cod` e `ind_cod`.
}
\description{
- Importa el Excel (web o local) y estandariza nombres/tipos.
- Filtra SIEMPRE a filas con `Unidad == "Puntaje del 0 al 10"`.
- Opcionalmente agrega `pilar_cod` (ECO, LAB, …) e `ind_cod` (ECO1, LAB3, …).
- Permite filtrar por edición/año/pilar/indicador/región usando códigos o nombres.
}
