% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indc_tabla.R
\name{indc_tabla}
\alias{indc_tabla}
\title{Tabla de indicadores por pilar (filas = regiones; columnas = indicadores×edición)}
\usage{
indc_tabla(
  ediciones,
  pilar,
  indicadores = "ALL",
  regiones = "ALL",
  usar_codigos = TRUE,
  incluir_peru = FALSE,
  largo = FALSE,
  gt = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{ediciones}{Vector numérico con una o más ediciones (p. ej. `2025` o `2018:2025`). **Obligatorio**.}

\item{pilar}{Cadena con el pilar (código o nombre), p. ej. `"LAB"` o `"Laboral"`. **Obligatorio**.}

\item{indicadores}{`"ALL"` (default) para todos los indicadores del pilar, o vector de códigos/nombres.
Se ignorará el indicador `"General"` salvo que se pida explícitamente.}

\item{regiones}{`"ALL"` (default) o vector de regiones (códigos, nombres o grupos `gr_*`; admite exclusiones con `-`).}

\item{usar_codigos}{Si `TRUE` (default), traduce códigos a nombres oficiales antes de filtrar.}

\item{incluir_peru}{`FALSE` (default). Si `TRUE`, incluye la fila `"Perú"`.}

\item{largo}{Si `TRUE`, devuelve un tibble **largo** con columnas `region, edicion, indicador, valor`.
La columna `indicador` usa el **código** cuando está disponible.}

\item{gt}{Si `TRUE`, devuelve una tabla `gt` con spanners por edición; si `FALSE` (default), devuelve `tibble`.}

\item{verbose}{Mostrar mensajes informativos (`FALSE` por defecto).}
}
\value{
- Si `largo = TRUE`: `tibble` con columnas `region, edicion, indicador, valor`.
- Si `largo = FALSE` y `gt = FALSE`: `tibble` ancho (filas regiones; columnas indicadores×edición).
- Si `largo = FALSE` y `gt = TRUE`: tabla `gt` formateada con spanners por edición.
}
\description{
Construye una tabla con los **indicadores** de un **pilar** del INCORE para
una o varias **ediciones**. Las filas son **regiones** y las columnas son
combinaciones *indicador × edición* (por ejemplo, `LAB2_2025`).
Puede devolver un `tibble` (por defecto), un `gt` con encabezados por edición
(spanners), o datos en **formato largo** para graficar.
}
\details{
La función parte de `leer_incore()` y:
- filtra por `pilar` (código o nombre),
- permite seleccionar `indicadores` (códigos o nombres; `"ALL"` para todos los del pilar),
- promedia si hubiera duplicados esporádicos,
- ordena regiones alfabéticamente,
- arma nombres de columnas `CODIGO_EDICION` (si hay código) o `indicador_limpio_EDICION`.
}
