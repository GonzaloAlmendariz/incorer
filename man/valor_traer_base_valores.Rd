% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/valor_helpers.R
\name{valor_traer_base_valores}
\alias{valor_traer_base_valores}
\title{Traer base para valores con filtros (NO filtra por unidad salvo que se pida)}
\usage{
valor_traer_base_valores(
  edicion = NULL,
  ediciones = NULL,
  anio = NULL,
  pilar = NULL,
  indicador = NULL,
  regiones = "ALL",
  unidad = "ALL",
  usar_codigos = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{edicion, ediciones, anio}{Filtros temporales.}

\item{pilar, indicador, regiones}{Filtros semánticos (códigos o nombres).}

\item{unidad}{character(1). `"ALL"` para no filtrar.}

\item{usar_codigos}{logical(1). Traduce códigos antes de filtrar.}

\item{verbose}{logical(1).}
}
\value{
tibble con columnas estandarizadas de `leer_total()`.
}
\description{
Envuelve a `leer_total()` aplicando normalizaciones mínimas y filtros.
No agrega; devuelve la base tal cual (tras filtros).
}
