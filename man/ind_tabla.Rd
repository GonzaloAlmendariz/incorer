% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ind_tabla.R
\name{ind_tabla}
\alias{ind_tabla}
\title{Tabla de INCORE por ediciones (general) o por pilares (lista para radar)}
\usage{
ind_tabla(
  ediciones,
  regiones = "ALL",
  modo = c("general", "pilares"),
  largo = FALSE,
  usar_codigos = TRUE,
  gt = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{ediciones}{Vector numérico con una o más ediciones (p. ej. `2025` o `2019:2025`).
En `modo = "pilares"` debe tener longitud 1.}

\item{regiones}{Vector de regiones (códigos, nombres o grupos `gr_*`). Use `"ALL"` para todas.}

\item{modo}{`"general"` o `"pilares"`.}

\item{largo}{Si `TRUE` y `modo="pilares"`, devuelve formato largo (`region, pilar, valor`).}

\item{usar_codigos}{Si `TRUE`, traduce códigos a nombres oficiales antes de filtrar.}

\item{gt}{Si `TRUE`, devuelve una tabla `gt`; en caso contrario, un `tibble`.}

\item{verbose}{Si `TRUE`, imprime mensajes informativos.}
}
\value{
Un `tibble` (por defecto) o un objeto `gt` si `gt = TRUE`.
}
\description{
Construye tablas a partir del INCORE reutilizando `leer_incore()`:
}
\details{
- **modo = "general"**: arma una tabla por *ediciones* (filas = regiones,
  columnas = `puntaje_YYYY`). Equivale a una versión flexible de `general_tabla()`.

- **modo = "pilares"** (una sola edición): arma los 6 pilares por región. Si
  `largo = TRUE`, devuelve tres columnas (`region`, `pilar`, `valor`)
  (ideal para `general_radar()`). **Nota:** la columna `edicion` se omite
  en la salida porque es redundante.

Siempre ordena alfabéticamente por `region`. En *modo general* los nombres
de columnas usan el prefijo `puntaje_YYYY`.
}
