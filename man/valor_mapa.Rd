% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/valor_mapa.R
\name{valor_mapa}
\alias{valor_mapa}
\title{Mapa coroplético del VALOR de un indicador por región (una edición)}
\usage{
valor_mapa(
  edicion,
  indicador,
  pilar = NULL,
  regiones = "ALL",
  usar_codigos = TRUE,
  mapa_sf = NULL,
  paleta = c("blues", "viridis", "cividis", "magma"),
  simplificar = 0,
  zoom = FALSE,
  expand_zoom = 0.04,
  etiquetas = c("repel", "texto", "ninguna"),
  subtitulo_nombre = TRUE
)
}
\arguments{
\item{edicion}{integer(1). Edición a graficar (2016–2025).}

\item{indicador}{character(1). Código (p.ej., "EDU2"/"ECO3") o nombre del indicador.}

\item{pilar}{character(1). Código o nombre del pilar. Opcional; se usa para validar/cofiltrar.}

\item{regiones}{"ALL" o vector de regiones (códigos o nombres).}

\item{usar_codigos}{logical(1). Si TRUE, traduce códigos en filtros.}

\item{mapa_sf}{Objeto sf con columnas `region` y `geometry`. Si NULL, usa `mapa_peru()`.}

\item{paleta}{"viridis","cividis","magma","blues" (para escala continua).}

\item{simplificar}{numeric. Tolerancia para `mapa_peru()`; 0 = sin simplificar.}

\item{zoom}{logical. Si TRUE y hay regiones selectas, hace zoom al bounding box.}

\item{expand_zoom}{numeric. Margen del bbox cuando `zoom=TRUE` (default 0.04).}

\item{etiquetas}{"repel","texto","ninguna". Cómo rotular los valores en el mapa.}

\item{subtitulo_nombre}{logical(1). Si TRUE (default) muestra el **nombre completo**
del indicador en el subtítulo; si FALSE, muestra el **código**.}
}
\value{
ggplot2
}
\description{
Colorea cada región según el **valor** (no puntaje) de un indicador
específico en una edición. Excluye "Perú" y todo registro cuya
\code{unidad} sea "Puntaje del 0 al 10".
}
