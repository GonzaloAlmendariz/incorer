% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/valor_tabla.R
\name{valor_tabla}
\alias{valor_tabla}
\title{Tabla de valores por indicador (filas = regiones; columnas = indicador×edición)}
\usage{
valor_tabla(
  ediciones,
  pilar,
  indicadores = "ALL",
  regiones = "ALL",
  usar_codigos = TRUE,
  incluir_peru = FALSE,
  largo = FALSE,
  gt = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{ediciones}{integer(). Una o más ediciones (p. ej. `2025` o `2018:2025`). **Obligatorio.**}

\item{pilar}{character(1). Código o nombre del pilar, p. ej. `"ECO"` o `"Entorno económico"`. **Obligatorio.**}

\item{indicadores}{"ALL" para todos los del pilar, o vector de códigos/nombres.
También admite entradas que empiecen con número (p.ej., `"3"` o `"3.1 ..."`) y las mapea a `ECO3` dentro de ese pilar.}

\item{regiones}{"ALL" (default) o vector de regiones (códigos o nombres).}

\item{usar_codigos}{logical(1). Si `TRUE`, traduce códigos antes de filtrar (pilar/regiones).}

\item{incluir_peru}{logical(1). Si `TRUE`, incluye la fila `"Perú"`. Default `FALSE`.}

\item{largo}{logical(1). Si `TRUE`, devuelve tibble **largo** (`region, edicion, indicador, valor, unidad, fuente`).}

\item{gt}{logical(1). Si `TRUE`, devuelve una tabla `gt` con spanners por edición; si `FALSE`, devuelve tibble.}

\item{verbose}{logical(1). Mensajes informativos.}
}
\value{
- Si `largo = TRUE`: `tibble` con columnas `region, edicion, indicador, valor, unidad, fuente`.
- Si `largo = FALSE` y `gt = FALSE`: `tibble` **ancho** (filas regiones; columnas indicador×edición).
- Si `largo = FALSE` y `gt = TRUE`: `gt` con spanners por edición.
}
\description{
Construye una tabla con los **valores** (no puntajes) de los **indicadores**
de un **pilar** del INCORE para una o varias **ediciones**.
Las filas son **regiones** y las columnas son combinaciones *indicador × edición*
(p.ej., `ECO3_2020`). Puede devolver:
- un `tibble` largo (si `largo=TRUE`),
- un `tibble` ancho (default),
- o un `gt` con spanners por edición (si `gt=TRUE`).
}
\details{
Siempre **excluye** `unidad == "Puntaje del 0 al 10"`.
}
