% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leer_total.R
\name{leer_total}
\alias{leer_total}
\title{Leer la base INCORE completa (web o local) con filtros opcionales}
\usage{
leer_total(
  ruta =
    "https://incore-spaces.nyc3.digitaloceanspaces.com/documents/1752079464_INCORE_2025_OpenData_1752079464.xlsx",
  hoja = 1,
  edicion = NULL,
  anio = NULL,
  pilar = NULL,
  indicador = NULL,
  region = NULL,
  unidad = "ALL",
  usar_codigos = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{ruta}{Ruta local o URL pública del Excel. Por defecto usa la URL 2025.}

\item{hoja}{Índice o nombre de la hoja del Excel (por defecto 1).}

\item{edicion}{(Opcional) Año(s) de edición (p.ej., 2016:2025). Numérico o carácter.}

\item{anio}{(Opcional) Año(s) de referencia del dato (p.ej., 2020:2024).}

\item{pilar}{(Opcional) Pilar(es), código(s) o nombre(s).}

\item{indicador}{(Opcional) Indicador(es), código(s) o nombre(s).}

\item{region}{(Opcional) Región(es), código(s) o nombre(s). Use \code{"ALL"} para todas.}

\item{unidad}{(Opcional) Cadena exacta a filtrar en la columna \code{unidad}.
Use \code{"ALL"} (default) para no filtrar. Ej.: \code{"Puntaje del 0 al 10"}.}

\item{usar_codigos}{Si \code{TRUE} (default), traduce códigos a nombres oficiales
(\code{catalogo_*()}) antes de filtrar.}

\item{verbose}{Si \code{TRUE}, imprime mensajes informativos.}
}
\value{
Un \code{tibble} con columnas estandarizadas:
\code{edicion}, \code{pilar}, \code{indicador}, \code{unidad}, \code{fuente},
\code{nota}, \code{region}, \code{etiqueta}, \code{valor}, \code{posicion}, \code{anio}.
}
\description{
Descarga/lee el Excel del INCORE (2025) y devuelve **todas** las filas,
incluidos registros cuya \code{unidad} no es "Puntaje del 0 al 10".
Admite filtros por \code{edicion}, \code{anio}, \code{pilar}, \code{indicador},
\code{region} y \code{unidad}. Útil para explorar "la base total".
}
\details{
A diferencia de las funciones \strong{general_*} e \strong{indc_*}, aquí
\emph{no} se filtra por unidad. Si lo deseas, puedes fijar
\code{unidad = "Puntaje del 0 al 10"} para quedarte sólo con puntajes.
}
\examples{
\donttest{
# Todo sin filtrar
# leer_total(verbose = FALSE)

# Sólo puntajes (0–10) del pilar Laboral en 2025
# leer_total(edicion = 2025, pilar = "LAB", unidad = "Puntaje del 0 al 10")

# Varias regiones por nombre/código y años de dato 2021–2024
# leer_total(anio = 2021:2024, region = c("MOQ","Arequipa","Lima*"))
}
}
