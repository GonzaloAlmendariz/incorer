% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_heatmap.R
\name{general_heatmap}
\alias{general_heatmap}
\title{Heatmap del puntaje General por región (múltiples ediciones)}
\usage{
general_heatmap(
  ediciones,
  regiones = "ALL",
  usar_codigos = TRUE,
  ordenar = c("ninguno", "por_ultimo", "por_promedio"),
  paleta = c("blues", "viridis", "cividis", "magma"),
  anotar = FALSE,
  mostrar_leyenda = TRUE
)
}
\arguments{
\item{ediciones}{Vector numérico con 2+ ediciones (p.ej., `2019:2025`). **Obligatorio**.}

\item{regiones}{Vector de regiones a incluir (códigos o nombres). Use `"ALL"` para todas.}

\item{usar_codigos}{Si `TRUE`, traduce códigos de regiones a nombres oficiales.}

\item{ordenar}{Criterio para ordenar las filas (regiones):
`"ninguno"` (alfabético), `"por_ultimo"` (por puntaje de la última edición),
`"por_promedio"` (promedio en el rango).}

\item{paleta}{Paleta de relleno: `"blues"` (default), `"viridis"`, `"cividis"`, `"magma"`.}

\item{anotar}{Si `TRUE`, escribe el valor dentro de cada celda.}

\item{mostrar_leyenda}{Si `TRUE`, muestra la leyenda de colores.}
}
\value{
Un objeto `ggplot2`.
}
\description{
Dibuja un mapa de calor donde las **filas** son regiones y las **columnas**
son ediciones. El color representa el puntaje **General** (0–10) del INCORE.
Solo usa las filas cuyo `pilar` comienza con *"Índice de Competitividad Regional"*.
}
\examples{
# Básico (todas las regiones), orden alfabético:
# general_heatmap(2020:2025)

# Ordenar por el puntaje de la última edición:
# general_heatmap(2020:2025, ordenar = "por_ultimo")

# Subconjunto de regiones y anotando valores:
# general_heatmap(2021:2025, regiones = c("MOQ","Lima*","Arequipa","Cusco"), anotar = TRUE)

}
