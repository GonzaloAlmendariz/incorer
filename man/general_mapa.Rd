% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_mapa.R
\name{general_mapa}
\alias{general_mapa}
\title{Mapa coroplético del puntaje General por región (una edición)}
\usage{
general_mapa(
  edicion,
  regiones = "ALL",
  usar_codigos = TRUE,
  mapa_sf = NULL,
  paleta = c("blues", "greens", "viridis", "cividis", "divergente"),
  simplificar = 0,
  zoom = FALSE,
  expand_zoom = 0.04,
  etiquetas = c("repel", "texto", "ninguna")
)
}
\arguments{
\item{edicion}{Entero de longitud 1 (p. ej., 2025). **Obligatorio**.}

\item{regiones}{Vector de regiones a incluir (códigos o nombres). `"ALL"` = todas.}

\item{usar_codigos}{Si `TRUE`, traduce códigos de regiones a nombres oficiales.}

\item{mapa_sf}{Objeto `sf` con `region` y `geometry`. Si `NULL`, se crea con `mapa_peru()`.}

\item{paleta}{Paleta para relleno: `"blues"` (default), `"viridis"`, `"cividis"`.}

\item{simplificar}{Tolerancia de simplificación al construir `mapa_peru()` (0 = sin simplificar).}

\item{zoom}{Si `TRUE` y hay `regiones` selectas, se hace zoom a su *bounding box*.}

\item{expand_zoom}{Margen proporcional del *bbox* cuando `zoom=TRUE`. Default `0.04`.}

\item{etiquetas}{`"repel"` (default), `"texto"`, `"ninguna"`.}
}
\value{
Objeto `ggplot2`.
}
\description{
Dibuja el mapa del Perú coloreando cada región según su puntaje **General** (0–10)
del INCORE para **una** edición. El subtítulo muestra: "Puntaje General (YYYY)".
Filtra **solo** los registros cuyo `pilar` comienza con
"Índice de Competitividad Regional".
}
