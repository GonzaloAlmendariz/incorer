% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_barras.R
\name{general_barras}
\alias{general_barras}
\title{Barras del puntaje General (total o por pilar) por región (una edición)}
\usage{
general_barras(
  edicion,
  pilar = NULL,
  regiones = "ALL",
  usar_codigos = TRUE,
  incluir_peru = FALSE,
  paleta = c("ipe", "okabe_ito", "viridis"),
  mostrar_leyenda = FALSE
)
}
\arguments{
\item{edicion}{integer(1). Edición a graficar (p. ej., 2025).}

\item{pilar}{NULL para el índice general total (default), o nombre/código
de un pilar para graficar su **General** (p. ej., "SAL", "Salud").}

\item{regiones}{Vector de regiones a incluir (códigos, nombres o grupos `gr_*`). Use `"ALL"` para todas.}

\item{usar_codigos}{logical. Si `TRUE`, traduce códigos a nombres oficiales.}

\item{incluir_peru}{logical. Si `TRUE`, incluye la fila `"Perú"`. Por defecto `FALSE`.}

\item{paleta}{character(1). Paleta cualitativa: `"ipe"` (default), `"okabe_ito"`, `"viridis"`.}

\item{mostrar_leyenda}{logical. Mostrar/ocultar leyenda (por defecto `FALSE`).}
}
\value{
Un objeto `ggplot2`.
}
\description{
Grafica barras (coord_flip) del puntaje General (0–10) para **una** edición.
Puedes elegir el **general total** (por defecto) o el **general de un pilar**
(por ejemplo, "Salud"). Ordena de mayor a menor y colorea por región con
paletas profesionales (~25 regiones).
}
